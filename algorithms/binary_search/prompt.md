# Binary Search

Implement:

```python
def binary_search(values: list[int], target: int) -> int:
    ...
```

## Input

`values` is a sorted list of integers in ascending order.

`target` is the integer to find.

## Output

Return the index of `target` in `values`.

Return `-1` if `target` is not present.
