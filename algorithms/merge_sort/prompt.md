# Merge Sort

Implement:

```python
def merge_sort(values: list[int]) -> list[int]:
    ...
```

## Input

`values` is a list of integers.

## Output

Return a new list containing the same integers in ascending order.

The input list should not be modified.
