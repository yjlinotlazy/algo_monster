# Heapsort

Implement:

```python
def heapsort(arr: list[int]) -> list[int]:
    ...
```

## Input

`arr` is a list of integers (can contain duplicates).

## Output

Return a new list containing the elements of `arr` in non-decreasing order.

The original list must not be modified.
